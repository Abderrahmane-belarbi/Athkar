import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.alphamade.athkar",
  appName: "Athkar",
  webDir: "out",
  server: { androidScheme: "https" },
};

export default config;
